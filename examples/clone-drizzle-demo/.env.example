# Pick one per phase (the demo script sets these for you):
# Source database (docker-compose):
DATABASE_URL=postgres://app:app@localhost:55432/appdb
# GFS clone (after `gfs clone ... --port 55433`):
# DATABASE_URL=postgres://postgres:postgres@localhost:55433/postgres
